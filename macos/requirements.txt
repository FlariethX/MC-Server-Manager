pywebview>=4.4.1
pyinstaller>=6.0

# Нативний WebKit-бекенд pywebview на macOS будується на PyObjC —
# на Windows/Linux ці пакети просто не встановляться (і не потрібні).
pyobjc-framework-Cocoa; sys_platform == "darwin"
pyobjc-framework-WebKit; sys_platform == "darwin"
pyobjc-framework-Quartz; sys_platform == "darwin"
